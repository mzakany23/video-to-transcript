[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: fast API key validation tests (~10 seconds)
    integration: full integration tests (requires API keys + test data)
