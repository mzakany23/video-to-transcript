# DEPRECATED: Use root Dockerfile instead
# Build with: docker build --target worker -t transcription-worker .
#
# This file is kept for reference but should not be used.
# The new monorepo build uses the root Dockerfile with multi-stage builds.

FROM python:3.11-slim

# Placeholder - redirects to root build
RUN echo "❌ This Dockerfile is deprecated!" && \
    echo "✅ Use root Dockerfile instead:" && \
    echo "   docker build --target worker -t transcription-worker ." && \
    exit 1