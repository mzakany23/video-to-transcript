apiVersion: v1
kind: Service
metadata:
  name: transcription-api
  namespace: endpoints
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app.kubernetes.io/name: transcription-api